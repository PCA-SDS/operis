// Stylesheet stub for Jest.
//
// Components may import CSS directly (Next.js supports this, and
// `app/global-error.tsx` has to import the app stylesheet because it replaces
// the root layout instead of nesting inside it). Jest has no CSS loader, so it
// would try to parse the file as JavaScript and fail. Mapping every `.css`
// import here keeps those components testable without pulling in a real
// stylesheet processor.
module.exports = {}
