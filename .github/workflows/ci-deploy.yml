name: CI & Deploy

# ==============================================================================
# ONE workflow, one run per push.
# ==============================================================================
#                    ┌─ lint ────────┐
#                    ├─ typecheck ───┤
#   pull request ──► prepare ─┼─ guards ──────┼─► ci-required        (tests SCOPED)
#                    ├─ audit ───────┤
#                    └─ test ────────┘
#
#   merge queue  ──► the same fan-out, tests UNFILTERED, plus the image build
#   push to main ──► the same fan-out ──► build ──┬─► deploy staging
#                                                 │   (/opt/operis-staging)
#                                                 └─► deploy production
#                                                     (waits for a reviewer)
#
# The gate is six concurrent jobs behind one cache-warming `prepare`. It was a
# single sequential job whose wall clock was the SUM of every step; lint and
# typecheck results now arrive in about three minutes instead of after the
# 466-second test step. Tests dominate either shape, so this buys feedback
# latency and independently re-runnable jobs, not a proportional wall-clock cut.
#
# `ci-required` is the ONLY required status check. Branch protection cannot
# express a conditional job, and a list of job names has to be re-edited in
# repository settings every time a job is added — so one aggregator inspects
# every result instead.
#
# A pull request runs a SCOPED test selection (scripts/ci-test-scope.mjs); the
# merge queue runs the suite unfiltered before the commit becomes reachable from
# main. That ordering is what makes scoping the pull-request lane safe: a miss
# blocks a merge rather than turning main red.
#
# The image is built here, once, and pulled by the server. Building on the VPS
# needs ~4 GB of heap and would compete with the four other production stacks
# sharing that host.
#
# ONCE means once for BOTH environments. Staging and production pull the same
# digest; staging costs no build time at all. That works because the runner
# stage takes no environment-specific build args and the image is built with no
# .env, so every NEXT_PUBLIC_* sits at its default and all real configuration
# arrives at runtime from the stack's own .env. Introduce a build arg that
# differs per environment and you lose this property — staging would stop
# testing the artifact production runs.
#
# The two deploys are the same reusable workflow (deploy-target.yml) called
# twice; see the deploy-staging/deploy-production jobs at the bottom.
#
# Runners are stock `ubuntu-latest`. Every workflow inherited from upstream
# targets `blacksmith-4vcpu-ubuntu-2404`, a runner fleet this fork has no
# access to — none of them has ever completed a run here. Disable them
# (`gh workflow disable "<name>"`) or they queue and cancel on every push.
# ==============================================================================

on:
  push:
    branches: [main]
    # Docs-only commits on main should not trigger a 40-minute build + deploy.
    # Deliberately NOT applied to pull_request: a PR should always produce a
    # green/red signal, so it can become a required status check later.
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'apps/docs/**'
      - '.ai/**'
  pull_request:
    branches: [main]
  # The full, unfiltered lane. A pull request runs a SCOPED test selection, so the
  # exhaustive run has to happen somewhere before the commit lands — and the merge queue
  # is before, not after. A miss in the scoped lane blocks a merge here instead of
  # turning main red, which is the whole reason scoping the pull-request lane is safe.
  merge_group:
    branches: [main]
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Existing image tag to (re)deploy, e.g. sha-1a2b3c4. Empty = build this branch.'
        required: false
        default: ''
      skip_quality:
        description: 'Skip lint/typecheck/tests. Emergency use only.'
        type: boolean
        default: false
      skip_staging:
        description: 'Deploy straight to production without going through staging. Emergency use only.'
        type: boolean
        default: false

# One run at a time per ref. PR runs cancel on a new push (the old result is
# worthless). Runs on main never cancel — killing a deploy halfway through a
# migration is far worse than waiting.
concurrency:
  group: ci-deploy-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# Least privilege by default; each job widens only what it needs. This matters
# because the deploy job SHIPS ITS TOKEN to the VPS for a `docker login`. A
# workflow-wide `packages: write` would hand that host a token able to publish
# images to this repository; `packages: read` can only pull.
permissions:
  contents: read

env:
  NODE_VERSION: '24.19.0'
  REGISTRY: ghcr.io
  # Tag AND digest. The tag says which version a human is reading about; the digest is
  # what actually runs. This scanner executes inside the one job holding
  # `packages: write`, so resolving it by a floating tag would make "whatever
  # aquasecurity pushed most recently" arbitrary code in a privileged job.
  # Refresh with:
  #   crane digest ghcr.io/aquasecurity/trivy:<version>
  TRIVY_IMAGE: 'ghcr.io/aquasecurity/trivy:0.74.0@sha256:62b1e65e8869bc4b4c6aa4fa2b21595256c7c2f6018a9d9ad61caf87187c1969'

jobs:
  # ---------------------------------------------------------------------------
  # Decides whether this run needs a container image. Implemented as a job
  # rather than `on.<event>.paths` because that key filters the WHOLE workflow:
  # a docs-only PR must still run lint/typecheck/tests, it just does not need a
  # 10-minute image build. Plain `git diff` on purpose — the header's rule that
  # this workflow stays free of third-party actions applies here too.
  scope:
    name: Decide what to run
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      image: ${{ steps.decide.outputs.image }}
      translation: ${{ steps.decide.outputs.translation }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # The merge-base is needed to diff a PR against its target.
          fetch-depth: 0

      - id: decide
        env:
          EVENT: ${{ github.event_name }}
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          # The translation image bakes ~2GB of converted model weights into a
          # layer, so it is rebuilt only when the service that defines it
          # changes. Decided BEFORE the early exit below so that both the PR and
          # the push paths emit it - an unset output reads as false, which would
          # silently never rebuild the engine.
          if [ "$EVENT" = "pull_request" ]; then
            TRANSLATION_CHANGED="$(git diff --name-only "$BASE" "$HEAD")"
            # Herestring, not a pipe, for the reason documented below: under
            # pipefail `git diff | grep -q` reports failure ON A MATCH.
            if grep -qE '^services/translation/' <<<"$TRANSLATION_CHANGED"; then
              echo "translation=true"  >> "$GITHUB_OUTPUT"
            else
              echo "translation=false" >> "$GITHUB_OUTPUT"
            fi
          else
            # Push or manual dispatch: rebuild. The conversion layer is cached in
            # the registry, so an unchanged model is a cache hit rather than a
            # 2GB download.
            echo "translation=true" >> "$GITHUB_OUTPUT"
          fi

          # Anything that is not a PR (push to main, manual dispatch) always
          # builds: that is the artifact that gets deployed.
          if [ "$EVENT" != "pull_request" ]; then
            echo "image=true" >> "$GITHUB_OUTPUT"
            echo "not a pull request - image build required"
            exit 0
          fi

          # Paths that can break `docker build` without breaking lint/tests.
          # Miss one here and main goes red after a green PR, so this list errs
          # toward over-building.
          # Collected first, then matched with a herestring rather than a pipe.
          # `git diff ... | grep -q` is unsafe under `pipefail`: grep exits the
          # instant it matches, git diff takes SIGPIPE and returns 141, and the
          # pipeline reports failure ON A MATCH — silently skipping the image
          # build for exactly the changes that most need it.
          #
          # Every entry below is something the Dockerfile COPYs or `yarn build`
          # reads. The previous list omitted tsconfig*.json (Dockerfile:16),
          # scripts/ (Dockerfile:43-44,52 — `yarn build` runs
          # scripts/build-package.mjs), newrelic.js (Dockerfile:56), the jest
          # configs and eslint.config.mjs (Dockerfile:57-58). A PR touching only
          # those skipped the image build entirely and the first build of that
          # tree happened on main — the exact green-PR/red-main asymmetry this
          # gate exists to prevent.
          #
          # 2026-09-15: the same asymmetry, one layer down. The list was built
          # from "files the Dockerfile COPYs by name", which silently excluded
          # the thing the image is actually made of — the source `yarn build`
          # compiles. A pure `packages/*/src` or `apps/mercato/src` change
          # merged green having never been built as an image, and main was again
          # the first place the image was built. Patching entry-by-entry has now
          # failed twice, so both source trees are listed: anything `yarn build`
          # compiles gets a no-push image verification on the PR. What remains
          # excluded is what the image genuinely does not contain — `.ai/`,
          # `docs/`, `apps/docs/`, `*.md`, and `__tests__`/`__integration__`.
          # This trades runner time on most PRs for never discovering a broken
          # image on main. That trade is deliberate.
          CHANGED="$(git diff --name-only "$BASE" "$HEAD")"
          if grep -qE '^(Dockerfile|\.dockerignore|deploy/|docker/|scripts/|package\.json|yarn\.lock|\.yarnrc\.yml|turbo\.json|tsconfig(\.base)?\.json|newrelic\.js|jest\.config\.cjs|jest(\.dom)?\.setup\.ts|eslint\.config\.mjs|apps/mercato/(package\.json|next\.config\.ts)|packages/[^/]+/package\.json|packages/[^/]+/src/|apps/mercato/src/)' <<<"$CHANGED"; then
            echo "image=true" >> "$GITHUB_OUTPUT"
            echo "image-relevant paths changed - image build required"
          else
            echo "image=false" >> "$GITHUB_OUTPUT"
            echo "no image-relevant paths changed - skipping image build"
          fi

  # ---------------------------------------------------------------------------
  # The gate, as six concurrent jobs behind one cache-warming job.
  #
  # This was a single sequential `quality` job whose wall clock was the SUM of every
  # step: 15m53s cold, 8m31s warm. Unit tests were 466s of a 935s total, so lint and
  # typecheck results — 13s and 107s of work — arrived eight to sixteen minutes after
  # the push. Fanning them out puts every fast check in front of the reviewer in about
  # three minutes and leaves the test job as the only long pole.
  #
  # Wall clock does NOT drop proportionally, and pretending otherwise would be the
  # wrong reason to do this: tests dominate either shape. The win is feedback latency,
  # plus the fact that a failed job can now be re-run on its own.
  #
  # `prepare` exists to warm the three caches deterministically and to fail the run
  # early on a broken install or build. It is the ONLY job that saves a cache — see the
  # save-cache input on .github/actions/setup.
  prepare:
    name: Install and build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    if: ${{ !(github.event_name == 'workflow_dispatch' && inputs.skip_quality) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: ./.github/actions/setup
        with:
          node-version: ${{ env.NODE_VERSION }}
          build: 'true'
          save-cache: 'true'

  # ---------------------------------------------------------------------------
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    needs: [prepare]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: ./.github/actions/setup
        with:
          node-version: ${{ env.NODE_VERSION }}
          # eslint reads source, not build output, so this job skips the build entirely.
          build: 'false'

      - name: Lint
        # lint:check-graph first: turbo silently reports success when the task graph
        # resolves to zero packages, so a misconfigured filter would make "lint passed"
        # mean "lint never ran".
        timeout-minutes: 15
        run: |
          yarn lint:check-graph
          yarn lint

  # ---------------------------------------------------------------------------
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    needs: [prepare]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: ./.github/actions/setup
        with:
          node-version: ${{ env.NODE_VERSION }}
          # `generate` writes packages/core/generated/, which core's tsconfig `include`
          # covers. Without it tsc typechecks a tree missing its generated registries.
          build: 'true'

      - name: Typecheck
        # typecheck:serial, not typecheck. The root `typecheck` inherits turbo's global
        # concurrency of 32, and TypeScript 7 is the native compiler — each process
        # saturates every core, so 20+ at once thrashes the runner. Serial is not the
        # slow path: the whole repo typechecks in ~10s.
        timeout-minutes: 15
        run: yarn typecheck:serial

  # ---------------------------------------------------------------------------
  guards:
    name: Repo-wide guards
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    needs: [prepare]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: ./.github/actions/setup
        with:
          node-version: ${{ env.NODE_VERSION }}
          # The telemetry guards resolve sibling packages through dist/, and the app
          # guards read files under .mercato/generated/. Both fail on a clean checkout.
          build: 'true'

      - name: Run repo-wide audit guards (always, unfiltered)
        # Unconditional on purpose: turbo's filter selects PACKAGES, not paths,
        # so a filtered run silently skips guards that audit files outside the
        # changed package. scripts/repo-wide-guards.mjs is the single
        # enumeration; run `yarn test:repo-wide-guards --list` to see what it
        # currently covers rather than copying the list into this comment.
        timeout-minutes: 10
        run: yarn test:repo-wide-guards

      - name: Run repository script tests
        # Guards over the workflows, Dockerfile and release tooling themselves.
        # These live outside every package, so no turbo task covers them.
        timeout-minutes: 10
        run: yarn test:scripts

      - name: Scan for time bombs
        timeout-minutes: 10
        run: yarn check:time-bombs:fail

  # ---------------------------------------------------------------------------
  audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    needs: [prepare]
    # Restores the pull-request half of a pair that had been broken.
    # `.github/workflows/audit.yml` opens by describing itself as the "time-triggered
    # counterpart to the `audit` job in ci.yml" — and ci.yml no longer exists in this
    # repository, so nothing audited a dependency change until 06:17 UTC the following
    # day, on main, after the merge. Same script and same allowlist as the scheduled
    # half, so the two agree on what counts as a failure.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: ./.github/actions/setup
        with:
          node-version: ${{ env.NODE_VERSION }}
          build: 'false'

      - name: Audit dependencies for known CVEs
        id: audit
        # BLOCKING. It was reporting-only until the backlog it was waiting on was
        # cleared — the six accepted-but-unfixed advisories (@xmldom/xmldom, js-yaml x2,
        # nodemailer, sharp, svgo) are fixed, and `yarn audit:ci` now reports no
        # advisories at or above threshold. The condition this step's own note set for
        # making it blocking is met, so the `continue-on-error` is gone.
        #
        # What that means for a red run here: unlike the image scan, `audit:ci` has no
        # `--ignore-unfixed`. A newly published HIGH advisory blocks even when no patched
        # release exists yet, which is deliberate — the alternative is a gate that stays
        # quiet about exactly the advisories nobody can fix.
        #
        # The escape hatch is scripts/audit-ci-allowlist.json: an entry per GHSA with a
        # written justification and a re-check condition (see the two image-size entries
        # for the shape). Reach for that when a fix genuinely does not exist. Do NOT
        # restore `continue-on-error` to get a green run — that silences every future
        # advisory to clear one, which is the failure this step was built to avoid.
        timeout-minutes: 10
        run: |
          set -o pipefail
          yarn audit:ci 2>&1 | tee audit.log

      - name: Publish the audit result
        if: always()
        run: |
          {
            if [ "${{ steps.audit.outcome }}" = "success" ]; then
              echo "### Dependency audit: no advisories at severity \`high\` or above"
            else
              echo "### Dependency audit: advisories found (reporting-only, not blocking)"
              echo ""
              echo "Fix each, or allowlist it with a reason in \`scripts/audit-ci-allowlist.json\`."
              echo "This step becomes blocking by removing \`continue-on-error\` from the audit job."
              echo ""
              echo '```text'
              cat audit.log 2>/dev/null || echo "(no output captured)"
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # ---------------------------------------------------------------------------
  test:
    name: Unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    needs: [prepare]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # The scoped path diffs against the merge base. scripts/ci-test-scope.mjs
          # refuses to narrow when the base ref does not resolve — against a shallow
          # clone that means every pull request would run the full suite, which is safe
          # but silently discards the narrowing this job exists for.
          fetch-depth: 0

      - uses: ./.github/actions/setup
        with:
          node-version: ${{ env.NODE_VERSION }}
          build: 'true'

      # --- FULL LANE ---------------------------------------------------------
      - name: Unit tests (full, unfiltered)
        # Everything that is not a pull request: the merge queue, a push to main, a
        # manual dispatch. This is the lane that has to be exhaustive, because it is the
        # last one before a commit becomes reachable from main.
        #
        # test:ci wraps `yarn test` with a single retry that fires ONLY when a jest
        # worker died on a fatal signal and no test reported a failure. A real
        # regression fails on assertions and is never retried. The retry is cheap
        # because `test` is a cached turbo task: packages that already passed replay,
        # and only the crashed one runs again.
        if: ${{ github.event_name != 'pull_request' }}
        timeout-minutes: 40
        env:
          JEST_CACHE_DIR: ${{ runner.temp }}/jest-cache
        run: yarn test:ci

      # --- SCOPED LANE -------------------------------------------------------
      - name: Unit tests (scoped to the change)
        # ONE entry point on purpose. This runs both halves of the scoped lane —
        # turbo's affected-package selection for every package except core, then a
        # jest run narrowed by import graph for core itself — and both always run, so
        # one push surfaces every failure.
        #
        # It is deliberately NOT a `yarn <script>`: scripts/__tests__/validation-gate-parity.test.mjs
        # requires every yarn script CI runs to appear in validation.commands, and a
        # NARROWED test command has no business in the documented local gate. The gate
        # documents `yarn test:ci`, the exhaustive superset this narrows, which is the
        # safe direction for that guard to be wrong in.
        #
        # Every uncertain input — an unresolvable base ref, an unsafe path, a file that
        # participates in DI or registry wiring — falls back to the full core suite.
        if: ${{ github.event_name == 'pull_request' }}
        timeout-minutes: 40
        env:
          JEST_CACHE_DIR: ${{ runner.temp }}/jest-cache
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: node scripts/ci-test-scope.mjs --base "$BASE_SHA"

      - name: Diagnose a failed run
        if: failure()
        run: |
          echo "::group::disk"
          df -h
          echo "::endgroup::"
          echo "::group::memory"
          free -m
          echo "::endgroup::"
          echo "::group::kernel OOM kills"
          # The signature of a worker killed by the OOM killer rather than by a
          # failing assertion. Empty output here means memory was NOT the cause.
          sudo dmesg 2>/dev/null | grep -iE 'out of memory|oom-kill|killed process' | tail -20 || echo "none"
          echo "::endgroup::"

  # ---------------------------------------------------------------------------
  # The single required status check.
  #
  # Branch protection cannot express a conditional job: naming `build` directly leaves a
  # docs-only pull request pending forever, because a skipped job never reports a
  # conclusion. And a required-checks list built from job names has to be edited in
  # repository settings every time a job is added — a step that is always forgotten, and
  # whose omission silently widens what can merge.
  #
  # One aggregator solves both. It is the only name in the ruleset, it inspects every
  # result explicitly, and `skipped` is accepted ONLY because each gate job's own `if:`
  # is the thing that decides whether it should have run. A job that failed, was
  # cancelled, or never reported fails this.
  ci-required:
    name: ci-required
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    if: ${{ always() }}
    needs: [scope, prepare, lint, typecheck, guards, audit, test]
    steps:
      - name: Verify every gate job succeeded
        env:
          RESULTS: ${{ toJSON(needs) }}
        run: |
          set -euo pipefail
          echo "$RESULTS"

          FAILED="$(echo "$RESULTS" | jq -r 'to_entries
            | map(select(.value.result != "success" and .value.result != "skipped"))
            | .[].key')"

          if [ -n "$FAILED" ]; then
            echo "::error::These gate jobs did not pass:"
            echo "$FAILED" | while read -r job; do echo "::error::  $job"; done
            exit 1
          fi

          echo "All gate jobs passed."

  # ---------------------------------------------------------------------------
  build:
    name: Build and push image
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      # A PR build never pushes, so it never needs write. Narrowed at runtime
      # is not possible, so this stays `write` and the push itself is gated on
      # the event below.
      packages: write
    needs: [scope, ci-required]
    # `needs: ci-required` (was `needs: quality`). The header used to argue for running this
    # concurrently so deploy waits on max(quality, build) instead of the sum —
    # correct arithmetic, wrong inputs now. Quality was ~20 minutes when that
    # was written; with a cached test task it is ~5, so gating costs a few
    # minutes of deploy latency and buys back every doomed build. The
    # 2026-08-29 push burned an hour of runner time on an image that could
    # never ship, because quality had already failed 40 minutes earlier.
    #
    # `always()` so an explicitly skipped gate (skip_quality, which cascades from
    # `prepare` through every job that needs it) still builds.
    if: >-
      always()
      && needs.scope.outputs.image == 'true'
      && (needs['ci-required'].result == 'success' || needs['ci-required'].result == 'skipped')
      && (github.event_name != 'workflow_dispatch' || inputs.image_tag == '')
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      image: ${{ steps.meta.outputs.image }}
      digest: ${{ steps.build.outputs.digest }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # 2026-08-29: main went red with "No space left on device" while the PR
      # that introduced the change was green — the PR skips this job unless an
      # image-relevant path changed, so the first build of that tree happened
      # here. Nothing about the image was wrong; a standard runner has ~14 GB
      # free and this build needs more than that once the layer cache misses:
      # yarn install (~440 MiB of packages), a full Next build, the BuildKit
      # layer store, and then `--cache-to mode=max` writing every builder layer
      # out again.
      #
      # The runner ships toolchains this image never uses. Reclaiming them is
      # the cheapest honest fix — it gives the build the headroom it actually
      # needs instead of trimming what the image contains. `|| true` on each
      # path so a future runner image that drops one of these does not fail the
      # build. The df lines stay so the next disk failure is one glance to
      # diagnose rather than an unhandled IOException.
      - name: Reclaim runner disk
        run: |
          set -uo pipefail
          echo "::group::disk before"
          df -h /
          echo "::endgroup::"
          sudo rm -rf /usr/local/lib/android || true      # ~9 GB
          sudo rm -rf /usr/share/dotnet || true           # ~1.6 GB
          sudo rm -rf /opt/ghc || true                    # ~1 GB
          sudo rm -rf /usr/local/share/boost || true
          sudo rm -rf /usr/share/swift || true
          sudo rm -rf "${AGENT_TOOLSDIRECTORY:-/opt/hostedtoolcache}" || true
          docker image prune -af || true
          echo "::group::disk after"
          df -h /
          echo "::endgroup::"

          # Measured 2026-08-30 on this tree: `docker buildx du` reports 10.03 GB
          # of BuildKit store for a single `--target runner` build, before the
          # checkout (~0.5 GB), the `--cache-from` import and the `mode=max`
          # export staging are counted. A stock runner offers ~14 GB free, which
          # is why this build began dying once the registry buildcache filled.
          #
          # Assert the headroom rather than discover its absence as a dead
          # runner. When the runner is killed by ENOSPC the worker cannot even
          # flush its own diagnostic log, so the job reports failure with NO log
          # and NO failed step — the 2026-08-29 outage was invisible for exactly
          # this reason. 20 GB is twice the measured requirement: comfortably
          # above real usage, low enough to fire before the build dies.
          # Falls back to an empty string if a future runner ships a df without
          # --output. A guard must never itself become the thing that breaks the
          # build, so an unreadable measurement warns and continues rather than
          # failing a build that would otherwise have succeeded.
          AVAIL_GB="$(df -B1G --output=avail / 2>/dev/null | tail -1 | tr -dc '0-9')"
          if [ -z "$AVAIL_GB" ]; then
            echo "::warning::could not read available disk; skipping the headroom check"
            AVAIL_GB=999
          fi
          echo "available after reclaim: ${AVAIL_GB}GB"
          if [ "$AVAIL_GB" -lt 20 ]; then
            echo "::error::only ${AVAIL_GB}GB free after reclaiming runner disk; this build needs ~11GB plus cache export headroom."
            echo "::error::The runner image likely changed what it ships. Check the 'disk before'/'disk after' groups above and extend the reclaim list."
            exit 1
          fi

      - name: Compute image coordinates
        id: meta
        env:
          EVENT: ${{ github.event_name }}
        run: |
          set -euo pipefail
          IMAGE="${REGISTRY}/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')"
          TAG="sha-${GITHUB_SHA::7}"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG"     >> "$GITHUB_OUTPUT"
          # A PR proves the image still builds; it must not publish a tag or
          # overwrite the shared buildcache, or a broken branch would poison
          # main's cache and `:latest` would point at unreviewed code.
          #
          # merge_group is the same case and for the same reason. A queued commit is
          # not yet reachable from main and may still be dequeued, so publishing
          # `:latest` from it would point the tag at code that never merged.
          if [ "$EVENT" = "pull_request" ] || [ "$EVENT" = "merge_group" ]; then
            echo "push=false" >> "$GITHUB_OUTPUT"
            echo "Verifying $IMAGE:$TAG builds (no push)"
          else
            echo "push=true" >> "$GITHUB_OUTPUT"
            echo "Building and pushing $IMAGE:$TAG"
          fi

      - name: Log in to GHCR
        run: echo '${{ secrets.GITHUB_TOKEN }}' | docker login "$REGISTRY" -u '${{ github.actor }}' --password-stdin

      - name: Create buildx builder
        # The docker-container driver is what makes --cache-to work. Using the
        # CLI directly keeps this workflow free of third-party actions.
        run: docker buildx create --name operis --driver docker-container --use

      - name: Build and push
        id: build
        timeout-minutes: 50
        env:
          IMAGE: ${{ steps.meta.outputs.image }}
          TAG: ${{ steps.meta.outputs.tag }}
          PUSH: ${{ steps.meta.outputs.push }}
        run: |
          set -euo pipefail

          ARGS=(
            --file Dockerfile
            --target runner
            --platform linux/amd64
            --tag "$IMAGE:$TAG"
            --cache-from "type=registry,ref=$IMAGE:buildcache"
            --metadata-file /tmp/build-meta.json
          )

          # Provenance and an SBOM on every published tag. Two flags, and they are what
          # let a later "which commit produced the image running in production, and what
          # is inside it" be answered from the registry instead of from memory.
          # Attestations attach to the pushed manifest, so they are meaningless for the
          # cacheonly PR path below.
          if [ "$PUSH" = "true" ]; then
            ARGS+=(--provenance=mode=max --sbom=true)
          fi

          if [ "$PUSH" = "true" ]; then
            # mode=max, and it has to be. mode=min exports only the layers
            # of the FINAL image — but this builds `--target runner`, a stage
            # that COPYs artifacts out of `builder` rather than containing its
            # layers. Under min, the expensive work (yarn install, then yarn
            # build) never reaches the registry cache at all, and the workflow
            # creates a fresh buildx builder each run so there is no local
            # cache to fall back on: every build would redo ~10 minutes of
            # install and compile even when nothing relevant changed.
            #
            # max costs ~81s of cache export per build and buys back the
            # install layer on every push that does not touch package.json,
            # yarn.lock or turbo.json — which is most pushes. Those three are
            # copied at Dockerfile:15, before `yarn install`, so touching any
            # of them invalidates everything after it and forces a full
            # rebuild regardless of cache mode.
            #
            # ignore-error=true: the cache export runs AFTER the image push, so
            # without it a registry hiccup or a full disk during the export
            # fails the step for an image that already shipped correctly —
            # turning a good release into a red main and a skipped deploy. The
            # cache is an optimisation; losing it must never lose the build.
            ARGS+=(
              --tag "$IMAGE:latest"
              --cache-to "type=registry,ref=$IMAGE:buildcache,mode=max,ignore-error=true"
              --push
            )
          else
            # PR: prove it builds, publish nothing, write no cache.
            ARGS+=(--output type=cacheonly)
          fi

          docker buildx build "${ARGS[@]}" .

          if [ "$PUSH" = "true" ]; then
            DIGEST="$(jq -r '."containerimage.digest"' /tmp/build-meta.json)"
          else
            DIGEST=""
          fi
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Scan the image for known vulnerabilities
        # Nothing inspected the runtime image before this. The scan runs against the
        # image built above and fails on HIGH/CRITICAL that HAVE A FIX AVAILABLE —
        # `--ignore-unfixed`, because failing a build for a CVE with no patch published
        # gives a team no action to take and trains them to bypass the gate.
        #
        # Pinned by digest, not by tag: this step runs with `packages: write` in scope,
        # and a scanner image resolved by a floating tag is arbitrary code in that job.
        # Trivy's own database download is allowed to fail the step — a scanner that
        # silently passes because it could not fetch advisories is worse than no scan.
        id: scan
        timeout-minutes: 15
        env:
          IMAGE: ${{ steps.meta.outputs.image }}
          TAG: ${{ steps.meta.outputs.tag }}
          PUSH: ${{ steps.meta.outputs.push }}
          DIGEST: ${{ steps.build.outputs.digest }}
          # Trivy runs inside its own container, so the `docker login` performed on
          # the HOST is invisible to it — its config lives in the runner's home, not
          # the scanner's. Without these two the remote lookup 401s and the scan dies
          # with "unable to find the specified image", which is how this step failed
          # every time it ran: never a finding, always a lookup it could not perform.
          TRIVY_USERNAME: ${{ github.actor }}
          TRIVY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          # The PR path builds with `--output type=cacheonly`, so nothing is published
          # to scan. Rebuilding it here purely to scan would double a ten-minute build;
          # the merge-queue and push paths publish a real image and are scanned.
          if [ "$PUSH" != "true" ]; then
            echo "PR build published no image (cacheonly) — scan runs on the merge-queue and push paths."
            exit 0
          fi

          # Read the image back from the REGISTRY, not the local daemon. The build
          # step ships it with `--push`, which streams straight to GHCR and loads
          # nothing locally, so `docker inspect` never had anything to find. Adding
          # `--load` instead would export a second full copy of a multi-gigabyte
          # image onto a runner that already needs a "Reclaim runner disk" step.
          #
          # By digest when we have one: that is the exact artifact this run pushed,
          # and unlike a tag it cannot be moved out from under the scan.
          TARGET="$IMAGE:$TAG"
          if [ -n "${DIGEST:-}" ]; then
            TARGET="$IMAGE@$DIGEST"
          fi
          echo "Scanning $TARGET"

          # No docker.sock. Nothing local is being inspected any more, and mounting
          # it handed a container root-equivalent control of the runner — in a job
          # that also holds `packages: write`.
          #
          # --platform pins the manifest to select: `--provenance`/`--sbom` make the
          # pushed tag an OCI index carrying attestation manifests alongside the image.
          docker run --rm \
            -e TRIVY_USERNAME \
            -e TRIVY_PASSWORD \
            -v "$HOME/.cache/trivy:/root/.cache/trivy" \
            "$TRIVY_IMAGE" \
            image \
              --severity HIGH,CRITICAL \
              --ignore-unfixed \
              --exit-code 1 \
              --no-progress \
              --scanners vuln \
              --platform linux/amd64 \
              "$TARGET"

      - name: Summary
        run: |
          {
            echo "### Image ${{ steps.meta.outputs.push == 'true' && 'built and pushed' || 'build verified (not pushed)' }}"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| image | \`${{ steps.meta.outputs.image }}\` |"
            echo "| tag | \`${{ steps.meta.outputs.tag }}\` |"
            echo "| digest | \`${{ steps.build.outputs.digest }}\` |"
            echo "| commit | ${{ github.sha }} |"
          } >> "$GITHUB_STEP_SUMMARY"

  # ---------------------------------------------------------------------------
  translation-image:
    name: Build and push translation image
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      packages: write
    needs: [scope]
    # Built independently of the app image. The weights are baked in at build
    # time, so this layer only changes when the service or its model does — and
    # rebuilding 2GB of converted weights on every application commit would burn
    # runner time for nothing.
    if: >-
      always()
      && needs.scope.result == 'success'
      && needs.scope.outputs.translation == 'true'
    outputs:
      image: ${{ steps.meta.outputs.image }}
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Resolve image name
        id: meta
        env:
          EVENT: ${{ github.event_name }}
        run: |
          set -euo pipefail
          IMAGE="${REGISTRY}/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')-translation"
          TAG="sha-${GITHUB_SHA::7}"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG"     >> "$GITHUB_OUTPUT"
          if [ "$EVENT" = "pull_request" ]; then
            echo "push=false" >> "$GITHUB_OUTPUT"
          else
            echo "push=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to GHCR
        run: echo '${{ secrets.GITHUB_TOKEN }}' | docker login "$REGISTRY" -u '${{ github.actor }}' --password-stdin

      - name: Create buildx builder
        run: docker buildx create --name translation --driver docker-container --use

      - name: Build and push
        id: build
        env:
          IMAGE: ${{ steps.meta.outputs.image }}
          TAG: ${{ steps.meta.outputs.tag }}
          PUSH: ${{ steps.meta.outputs.push }}
        run: |
          set -euo pipefail
          # The model-conversion stage is the expensive one and changes only when
          # the model id or quantisation does, so the registry cache carries it
          # across builds. `ignore-error` because a cache write failure must not
          # fail a build that otherwise succeeded.
          ARGS=(
            --file services/translation/Dockerfile
            --tag "$IMAGE:$TAG"
            --cache-from "type=registry,ref=$IMAGE:buildcache"
            --progress plain
          )
          if [ "$PUSH" = "true" ]; then
            ARGS+=(--tag "$IMAGE:latest" --push
                   --cache-to "type=registry,ref=$IMAGE:buildcache,mode=max,ignore-error=true")
          else
            ARGS+=(--output type=cacheonly)
          fi
          docker buildx build "${ARGS[@]}" services/translation

      - name: Summary
        if: always()
        run: |
          {
            echo "### Translation image ${{ steps.meta.outputs.push == 'true' && 'built and pushed' || 'build verified (not pushed)' }}"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| image | \`${{ steps.meta.outputs.image }}\` |"
            echo "| tag | \`${{ steps.meta.outputs.tag }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

  # ---------------------------------------------------------------------------
  # Deployment: staging, then production behind an approval.
  #
  # Both calls hand the SAME digest to the same reusable workflow
  # (deploy-target.yml); only the inputs differ. Nothing is rebuilt between
  # them, so "it worked on staging" is a statement about the exact bytes that
  # production will run, not about a similar build of the same commit.
  #
  # The gate is the required reviewer on the `production` environment, not
  # anything expressed here. That is deliberate: branch protection and job
  # conditions cannot express "a human looked at it", and an environment
  # reviewer can be changed without editing this file.
  #
  # NOTE ON QUEUEING: concurrency.group is per-ref with cancel-in-progress
  # false on main, so while a production approval is pending the run stays
  # open and the next merge to main queues behind it. That is the cost of the
  # gate. If it becomes a problem, give the two deploys separate concurrency
  # groups rather than removing the approval.
  # ---------------------------------------------------------------------------
  deploy-staging:
    name: Staging
    needs: [scope, ci-required, build, translation-image]
    # `always()` so an explicitly skipped dependency (skip_quality, or a build
    # skipped when redeploying an existing tag) still deploys. Each result is
    # then checked by name: success or skipped is fine, failure is not.
    if: >-
      always()
      && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
      && !inputs.skip_staging
      && needs.scope.result == 'success'
      && (needs['ci-required'].result == 'success' || needs['ci-required'].result == 'skipped')
      && (needs.build.result == 'success'   || needs.build.result == 'skipped')
      && (needs['translation-image'].result == 'success' || needs['translation-image'].result == 'skipped')
    # The workflow-level default is `contents: read`. A called workflow's job
    # permissions are CAPPED by the caller's, so omitting packages here would
    # silently strip the called job's `packages: read` and the VPS pull would
    # fail with what looks like a registry problem.
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/deploy-target.yml
    secrets: inherit
    with:
      environment: staging
      app_dir: /opt/operis-staging
      stack_name: operis-staging
      skip_backup: true
      requested_tag: ${{ inputs.image_tag }}
      built_tag: ${{ needs.build.outputs.tag }}
      built_image: ${{ needs.build.outputs.image }}
      built_digest: ${{ needs.build.outputs.digest }}

  # ---------------------------------------------------------------------------
  deploy-production:
    name: Production
    needs: [scope, ci-required, build, translation-image, deploy-staging]
    # Production requires staging to have SUCCEEDED — not merely "not failed".
    # `skipped` is accepted for every other dependency and deliberately not for
    # this one: a skipped staging deploy means the release was never run
    # anywhere, which is exactly the state this job exists to prevent.
    #
    # skip_staging is the emergency escape hatch, mirroring skip_quality. It is
    # a workflow_dispatch input, so it cannot be reached by a push.
    if: >-
      always()
      && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
      && needs.scope.result == 'success'
      && (needs['ci-required'].result == 'success' || needs['ci-required'].result == 'skipped')
      && (needs.build.result == 'success'   || needs.build.result == 'skipped')
      && (needs['translation-image'].result == 'success' || needs['translation-image'].result == 'skipped')
      && (needs['deploy-staging'].result == 'success'
          || (github.event_name == 'workflow_dispatch' && inputs.skip_staging))
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/deploy-target.yml
    secrets: inherit
    with:
      environment: production
      app_dir: /opt/operis
      stack_name: operis
      skip_backup: false
      requested_tag: ${{ inputs.image_tag }}
      built_tag: ${{ needs.build.outputs.tag }}
      built_image: ${{ needs.build.outputs.image }}
      built_digest: ${{ needs.build.outputs.digest }}
